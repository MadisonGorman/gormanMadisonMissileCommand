using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerBulletFunctionality : MonoBehaviour
{
    // Referenced: "2D Shooting in Unity (Tutorial)" by Brackeys
    //public float bulletSpeed = 15f;

    // Referenced: "2D Shooting in Unity (Tutorial)" by Brackeys
    //public Transform bulletTransform;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //float gradual = bulletSpeed * Time.deltaTime;

        //bulletTransform.position = Vector3.MoveTowards(bulletTransform.position, , gradual);
    }
}
